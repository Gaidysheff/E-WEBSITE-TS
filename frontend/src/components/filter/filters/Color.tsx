import React from "react";

interface Props {
  options: any;
}

const Color = ({ options }: Props) => {
  return (
    <>
      <div className="font-semibold my-2">Color</div>
    </>
  );
};

export default Color;
