export type Product = {
  id: number;
  image: string;
  title: string;
  price: number;
  // sale: number;
};

export type ThemeSwitch = "light" | "dark";
// export type ThemeSwitch = "light" | "dark" | null;
